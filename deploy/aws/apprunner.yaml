version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install --no-cache-dir -r requirements.txt
run:
  runtime-version: 3.12
  command: python admin/app.py
  network:
    port: 5050
  env:
    - name: PORT
      value: '5050'
    - name: KF_AUTO_BOOTSTRAP
      value: '1'
