# Azure Container Apps

Deploy the included Docker image to Azure Container Apps, expose port 8080, and inject secrets through Container Apps secrets. Use Blob Storage/PostgreSQL/Azure AI Search adapters for durable multi-replica state.
