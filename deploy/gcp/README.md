# Google Cloud Run

Build the included Dockerfile and deploy it to Cloud Run with container port 8080. Set `KF_HOST=0.0.0.0`, `KF_PORT=8080`, and secrets through Secret Manager. Use Cloud SQL/Cloud Storage/managed search adapters for durable multi-instance state.
