# Example: code intelligence

The bundled Python sample contains a `RetryPolicy` symbol. Ask:

> Where is RetryPolicy defined?

or use the agent tool contract directly with `find_symbol("RetryPolicy")`.

This demonstrates symbol-aware retrieval alongside ordinary semantic/lexical retrieval.
