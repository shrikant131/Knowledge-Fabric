# Example: multi-source agent

Register multiple sources with distinct IDs. Each source maintains its own configuration and state. Query the relevant source through its UI/API endpoint.

For a future unified enterprise search experience, the registry can be extended with a federated retrieval layer that applies ACLs and merges evidence across sources.
