# Example: Bedrock-backed generation

1. Configure AWS credentials.
2. Set `generator: bedrock`.
3. Set `llm_enabled: true`.
4. Set the Bedrock region/model.
5. Ingest and query.

The retrieval and evidence pipeline remains identical to local mode.
